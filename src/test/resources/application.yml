spring:
    datasource:
        url: jdbc:mysql://management.czchaiykemru.ap-northeast-2.rds.amazonaws.com:3306/koonsdiary?serverTimezone=Asia/Seoul
        username: koonda
        password: cucumber52
        driver-class-name: com.mysql.cj.jdbc.Driver
    output:
        ansi:
            enabled: ALWAYS
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                #show_sql: true
                format_sql: true
                dialecct: MySQLDialect

logging.level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

